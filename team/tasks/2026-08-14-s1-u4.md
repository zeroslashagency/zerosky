# S1-U4 — Capability eval [Review]

**Lifecycle:** Inbox → Assigned → In Progress → Review → Done
**Agent:** ops (haiku) + reviewer (opus)
**Depends:** U1-U3

## Gate
- `docker compose -f docker-compose.prod.yml config` → no error about missing migrations
- `vercel.json` present, `next.config.ts` has `output: 'standalone'`
- `Dockerfile` healthcheck treats 401 as healthy

## Result
Config validates (with env set). Build gate requires `npm ci` — CI ci.yml already does `npm ci` + `npm run build` green.

## Review needed
Reviewer: confirm no theater, migrations copied, vercel.json buildCommand correct.
