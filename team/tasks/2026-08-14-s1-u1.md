# S1-U1 — Fix .dockerignore migrations [Done]

**Lifecycle:** Inbox → Assigned → In Progress → Review → Done
**Agent:** builder-api (sonnet)
**Depends:** none

## Spec
`.dockerignore:69` excluded `prisma/migrations/**/migration.sql` from Docker context → `prisma migrate deploy` inside image has 0 migrations → 500 on first query.

## Done
- ` .dockerignore:62-70` — removed `prisma/migrations/**/migration.sql`, kept `packages/database/generated` + `packages/offline/generated` excluded.
- Verified `docker compose -f docker-compose.prod.yml config` still validates, migrations now copied via `COPY packages ./packages`.

## Verify
`docker build` context includes `packages/database/prisma/migrations/0_init/migration.sql` (check `docker build --progress=plain 2>&1 | grep migration`).

## Handoff
What: unblocked migrations for Hetzner deploy
Where: `.dockerignore`
Known: none
Next: U2 vercel.json + standalone
