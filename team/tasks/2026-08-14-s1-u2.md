# S1-U2 — Add vercel.json + standalone [Done]

**Lifecycle:** Inbox → Assigned → In Progress → Review → Done
**Agent:** builder-web (sonnet)
**Depends:** U1

## Spec
Vercel had no `vercel.json` → builds at repo root fail `turbo not found`. Hetzner Dockerfile used `npm start` without standalone, 1.2 GB, slow, no auto-migrate.

## Done
- `vercel.json` — `buildCommand npm run build`, `outputDirectory apps/pos-web/.next`
- `apps/pos-web/next.config.ts:4` — `output: 'standalone'`
- `entrypoint.sh` — `prisma migrate deploy` then `node apps/pos-web/server.js`
- `Dockerfile:62-90` — copy `standalone` + `static`, copy `packages` + `entrypoint.sh`, healthcheck respects `PORT` and treats 401 as healthy, `CMD ["./entrypoint.sh"]`.

## Verify
`npm run build --prefix apps/pos-web` (with `output:standalone`) produces `apps/pos-web/.next/standalone`. Vercel build uses `apps/pos-web/.next`.

## Handoff
What: Vercel+Hetzner hybrid now buildable
Where: `vercel.json`, `next.config.ts`, `Dockerfile`, `entrypoint.sh`
Known: `npm ci` still required before build (CI does it)
Next: U3 PgBouncer
