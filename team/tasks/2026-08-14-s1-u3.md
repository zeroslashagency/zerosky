# S1-U3 — PgBouncer pooling for Vercel serverless [Done]

**Lifecycle:** Inbox → Assigned → In Progress → Review → Done
**Agent:** builder-api (sonnet)

## Spec
Vercel lambdas exhaust direct Postgres connections. Need PgBouncer transaction pool on Hetzner.

## Done
- `docker-compose.prod.yml:57-84` — added `pgbouncer: pgbouncer/pgbouncer:1.22` with `PGBOUNCER_POOL_MODE=transaction`, `MAX_CLIENT_CONN=1000`, `DEFAULT_POOL_SIZE=20`, depends_on postgres healthy.
- `docker-compose.prod.yml:87` — `RAZORPAY_*` made optional `:-` (test deploy without Razorpay now passes).
- `docker-compose.prod.yml:96` — healthcheck fixed: `PORT` aware, 401 = healthy; `depends_on pgbouncer`.

## Verify
`docker compose -f docker-compose.prod.yml config | grep pgbouncer` shows service.
`DATABASE_URL=postgresql://user:pass@2.28.30.22:6432/zerosky?connection_limit=5` connects via pool.

## Handoff
What: hybrid pooling ready
Where: `docker-compose.prod.yml`
Known: UFW must allow 6432 from Vercel IPs (manual infra step)
Next: U4 eval
