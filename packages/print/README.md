# @zerosky/print — experimental

ESC/POS thermal receipt rendering (Rust). 84 tests, mock transport only.

Status: **experimental** — `listPrinters`/`printBill` not wired to UI in `apps/pos-web`.
See `docs/PRINTING.md`.
