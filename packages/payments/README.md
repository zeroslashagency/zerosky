# @zerosky/payments — experimental

Razorpay client + reconciliation (66 tests). Not wired to checkout UI yet.

Status: **experimental** — requires `RAZORPAY_KEY_*`. Leave empty in `.env` for test deploy.
