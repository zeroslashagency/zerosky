# Deprecated wrapper — canonical compose lives at the repo root (docker-compose.yml).
# `cd packages/database && docker compose up -d` still works via `include`.
# Do not add services here; edit the root file instead.
name: zerosky-dev

include:
  - path: ../../docker-compose.yml
